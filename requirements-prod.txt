# Production runtime dependencies only.
# Keep this file pinned so security scans and deploys resolve predictably.

fastapi==0.136.3
starlette==1.2.1
uvicorn[standard]==0.49.0
httpx==0.28.1
pydantic==2.13.4
Jinja2==3.1.6
itsdangerous==2.2.0
python-dotenv==1.2.2
